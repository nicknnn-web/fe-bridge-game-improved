## 1. 暴击伤害修复

- [x] 1.1 在 `attack()` 函数中，检查 `r.c`（暴击标志）
- [x] 1.2 若 `r.c === true`，伤害值 `r.v × 2`，伤害飘字显示橙色/金色，战斗日志标注 💥

## 2. 反击系统实现

- [x] 2.1 在 `attack()` 动画完成回调中，检查防御方是否可反击
- [x] 2.2 反击条件：`wb.r >= wa.r`（防御方武器射程 ≥ 攻击方武器射程）且非暴击
- [x] 2.3 调用 `attack(df, at)` 执行反向攻击
- [x] 2.4 反击时设置 `df.acted = true` 防止二次反击

## 3. 单位数值对齐

- [x] 3.1 重甲（p2, e2）MOV: 3 → 4
- [x] 3.2 弓手（p4, e4）MOV: 4 → 5
- [x] 3.3 更新 `SPEC.md` 中对应数值表

## 4. 起点位置对齐

- [x] 4.1 蓝方：p1→(1,9), p2→(2,9), p3→(1,10), p4→(2,10)
- [x] 4.2 红方：e1→(9,1), e2→(10,1), e3→(9,2), e4→(10,2)
- [x] 4.3 更新 `SPEC.md` 中对应起点坐标
